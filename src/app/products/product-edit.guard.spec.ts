import { TestBed, async, inject } from '@angular/core/testing';
//import { ProductsEditGuard } from './products-edit.guard';

describe('ProductsEditGuard', () => {
  beforeEach(() => {
    /*TestBed.configureTestingModule({
      providers: [ProductsEditGuard]
    });*/
  });

  it.skip('always passes', () => {
    expect(true).toBeTruthy();
  });

  /*it.skip('should ...', inject([ProductsEditGuard], (guard: ProductsEditGuard) => {
    expect(guard).toBeTruthy();
  }));*/
});
